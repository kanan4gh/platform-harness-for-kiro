---
inclusion: manual
---

# ドキュメント記入ガイド

このファイルは `#skill-doc-writing` で明示的に参照された時のみ読み込まれます。`docs/` 配下の各永続ドキュメントの記入ガイドを定義します。

## 共通原則

- 具体性と測定可能性: すべての要件を明確・数値で表現する
- ユーザー中心: 機能はユーザーの課題解決に紐づける
- 他ドキュメントとの整合性: 記入後に矛盾がないか確認する

---

## product-requirements.md（プロダクト要求定義書）

**必須セクション**:

| セクション | 内容 |
|---|---|
| プロダクト概要 | 名称・コンセプト・ビジョン・目的 |
| ターゲットユーザー | ペルソナ（基本属性・課題・期待する解決策） |
| 成功指標（KPI） | SMART 原則に基づく定量目標 |
| 機能要件 | ユーザーストーリー + 受け入れ条件（チェックリスト）+ 優先度（P0/P1/P2） |
| 非機能要件 | パフォーマンス・ユーザビリティ・信頼性 |

**優先度の定義**:
- P0（必須）: これがないとプロダクトとして成立しない
- P1（重要）: 初期リリース後すぐに追加すべき
- P2（できれば）: 将来的に検討

---

## functional-design.md（機能設計書）

**必須セクション**:

| セクション | 内容 |
|---|---|
| 設計指針 | 実装上の判断基準（3〜5項目） |
| システム構成図 | コンポーネント間の関係（テキスト図で可） |
| Claude CLI ↔ Kiro 概念マッピング | 両者の対応表（移行経験者向け） |
| コンポーネント設計 | 各層の詳細設計 |
| ワークフロー設計 | 主要なユースケースのフロー |

---

## architecture.md（技術仕様書）

**必須セクション**:

| セクション | 内容 |
|---|---|
| アーキテクチャ概要 | 層構造・情報フロー |
| 各層の設計 | 役割・設計方針・ファイル仕様 |
| 環境設定戦略 | devcontainer 非依存の代替手段 |
| 技術的制約 | バージョン依存・既知の制限 |
| ブートストラップ戦略 | フェーズ定義・移行条件 |
| テスト戦略 | 受け入れテスト・ドキュメント品質チェック |

---

## repository-structure.md（リポジトリ構造定義書）

**必須セクション**:

| セクション | 内容 |
|---|---|
| リポジトリ全体ツリー | 実際のディレクトリ構造（コードブロック） |
| ディレクトリ詳細 | 各ディレクトリの役割・配置ファイル・配置ルール |
| ファイル配置規則 | 新規ファイル作成時の判断フロー |
| テンプレートとしての使い方 | 必須変更・任意変更・変更しないもの |

---

## development-guidelines.md（開発ガイドライン）

**必須セクション**:

| セクション | 内容 |
|---|---|
| コーディング規約 | 言語別の命名規則・型定義・関数設計 |
| フックファイル品質基準 | kiro-cli 不使用・目的と動作の明確さ |
| PR・リリースフロー | ブランチ戦略・コミットメッセージ規約 |
| テスト戦略 | テストの種類・カバレッジ目標 |

---

## glossary.md（ユビキタス言語定義）

**記入形式**:

```markdown
## [用語]

**定義**: [1〜2文の定義]

**使用例**: [文脈での使い方]

**関連用語**: [他の用語へのリンク]
```

**収録対象**:
- プロジェクト固有の用語
- 一般用語だが本プロジェクトで特定の意味を持つもの
- チームメンバー間で定義が曖昧になりがちな用語

---

## LLM 向け定義ファイルの記述規約

フック（`.kiro/hooks/*.kiro.hook`）・steering ファイル・エージェント定義は LLM が実行する「手順書」であり、曖昧さが実行者依存の挙動差を生む。

### 設計の具体形を転記する

設計書にファイルパターン・コマンド・正規表現等の具体形がある場合、定義ファイルには**その具体形をそのまま転記**する。散文で言い換えない。

```markdown
❌ 悪い例: 「YYYYMMDD- 接頭辞の作業ディレクトリを列挙する」（散文のみ）
✅ 良い例: 「`.steering/` 直下の `YYYYMMDD-` 接頭辞ディレクトリ（パターン: `[0-9]*-*`）の
          `tasklist.md` を列挙する（`_template/` 等の規約外ディレクトリは含めない）」
```

理由: 散文だけだと実行者（LLM）が広いパターンを選び、規約外のファイルを誤って処理する事故が起きる（実例あり）。具体形+意図の併記が最も堅牢。

### 既存ファイルの用語に引っ張られない

既存ファイルへ追記する際、そのファイル内の既存用語が設計書と食い違っている場合は**設計書の用語を優先**する（既存の食い違いに合わせて複製しない）。用語の正典はドキュメント（用語集・設計書）にある。
