# 設計書

## 実装アプローチ

<!-- 例:
既存の UserService に `uploadAvatar(userId, file)` メソッドを追加する。
ファイルは S3 に保存し、URL を users テーブルの avatar_url カラムに保存する。
フロントエンドは既存の ProfilePage コンポーネントに画像アップロード UI を追加する。
-->
[実装の方針・アーキテクチャ上の決定を記述]

## 変更対象ファイル

<!-- 例:
| ファイル | 変更種別 | 内容 |
|---------|---------|------|
| `src/services/user_service.py` | 変更 | `upload_avatar()` メソッドを追加 |
| `src/api/users.py` | 変更 | POST /users/{id}/avatar エンドポイントを追加 |
| `tests/test_user_service.py` | 変更 | アップロード機能のテストを追加 |
-->

| ファイル | 変更種別 | 内容 |
|---------|---------|------|
| [ファイルパス] | [新規作成 / 変更 / 削除] | [変更内容] |

## 技術的判断と根拠

<!-- 例:
| 判断 | 根拠 |
|------|------|
| S3 を使用 | プロジェクトが既に AWS を使用しており、追加コストなく利用できる |
| サムネイル生成はしない | MVP スコープ外のため、将来の Issue で対応する |
-->

| 判断 | 根拠 |
|------|------|
| [技術的な選択・設計判断] | [その根拠・理由] |

## 実装の順序

<!-- 例:
1. `UserService.upload_avatar()` を実装しユニットテストを追加
2. API エンドポイント `POST /users/{id}/avatar` を実装
3. フロントエンドのアップロード UI を実装
4. E2E テストで動作確認
-->

1. [実装ステップ1]
2. [実装ステップ2]
3. [実装ステップ3]

<!-- この順序を tasklist.md のフェーズ1タスクに展開してください -->
